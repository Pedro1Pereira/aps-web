/** @format */

const port = 4002;
const address = 'http://localhost';
const base_url = '/aps-web';
const fullAddress = address + ':' + port + base_url;

module.exports = {
    port,
    address,
    fullAddress,
    base_url
};
